// theme.js
import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
    colors: {
        primary: "#008427",   // Example color
        secondary: "#D20428",
        tertiary: "#F7BD00",
        quaternary: "#FFFFFF"
    }
});

export default theme;
