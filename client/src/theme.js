import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
    colors: {
        primary: "#E2E8F0",
        secondary: "#D20428",
        tertiary: "#F7BD00",
        quaternary: "#FFFFFF"
    }
});

export default theme;
